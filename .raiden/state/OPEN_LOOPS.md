# Open Loops

## v1.0.0 push pending — awaiting Contents:write token

All v1.0.0 changes (source, README, state files) are committed locally. Remote push requires a fresh Contents:write scoped PAT; operator to provide token to complete push.

## Operator live test pending

Windhawk install and live test not yet run. Operator to test against a fullscreen RDP session on Windows 11 24H2. Key verification required for v1.0.0: ensure the button scales properly on a HiDPI (e.g. 4K 200%) monitor and verify hotkey conflict reporting if forced. Report results back for any follow-up work.

